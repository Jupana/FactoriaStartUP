<!--Ventana Pop-Up -->  
    <div class="modal fade par" id="modalForm_necesito" role="dialog">
        <div class="modal-dialog">
            <div class="modal-content">
                <!-- Modal Header -->
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">
                        <span class="modal-close" aria-hidden="true">×</span>
                        <span class="sr-only">Close</span>
                    </button>
                </div>
                <div class="col-md-12 text-center m-t-10  modal-title">
                    <h4 class="par" id="myModalLabel">Nuevo Perfil Necesitado</h4>  
                </div>    
                <!-- Modal Body -->
                <div class="modal-body">                                
                            {{ form_start (form_New_Proyect_Needs, { 'attr' : { 'id': 'project_needs-liviu' } }) }}
                                    {{ form_row  (form_New_Proyect_Needs.needs_perfil) }}
                                    {{ form_row (form_New_Proyect_Needs.needs_percent) }}  
                                    {{ form_row (form_New_Proyect_Needs.needs_description) }}                                             
                                     <input type="submit" id="submit-form" class="btn-primary float-l" style="display:none"/>  
                            {{ form_end (form_New_Proyect_Needs) }}
                                    <label for="submit-form" class="btn-primary float-l" tabindex="0">Guardar perfil</label>

                </div>
                <!-- Modal Footer -->
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-success submitBtn" onclick="submitContactForm()">Guardar</button>
                </div>
            </div>
        </div>
    </div>
    <script type="text/javascript">
        function submitContactForm(){
            var reg = /^[A-Z0-9._%+-]+@([A-Z0-9-]+.)+[A-Z]{2,4}$/i;
            var perfil = $('#perfil').val();
            var sector = $('#acuerdo').val();
            var perfilDesc = $('#perfilDesc').val();
            if(name.trim() == '' ){
                alert('Please enter your perfil.');
                $('#perfil').focus();
                return false;
            }else if(email.trim() == '' ){
                alert('Please enter your acuerdo.');
                $('#acuerdo').focus();
                return false;
            }else if(email.trim() != '' && !reg.test(email)){
                alert('Please enter valid email.');
                $('#acuerdo').focus();
                return false;
            }else if(message.trim() == '' ){
                alert('Please enter your description.');
                $('#perfilDesc').focus();
                return false;
            }else{
                $.ajax({
                    type:'POST',
                    url:'submit_form.php',
                    data:'contactFrmSubmit=1&perfil='+perfil+'&acuerdo='+acuerdo+'&perfilDesc='+perfilDesc,
                    beforeSend: function () {
                        $('.submitBtn').attr("disabled","disabled");
                        $('.modal-body').css('opacity', '.5');
                    },
                    success:function(msg){
                        if(msg == 'ok'){
                            $('#perfil').val('');
                            $('#acuerdo').val('');
                            $('#perfilDesc').val('');
                            $('.statusMsg').html('<span style="color:green;">Thanks for contacting us, well get back to you soon.</p>');
                        }else{
                            $('.statusMsg').html('<span style="color:red;">Some problem occurred, please try again.</span>');
                        }
                        $('.submitBtn').removeAttr("disabled");
                        $('.modal-body').css('opacity', '');
                    }
                });
            }
        }
    </script>