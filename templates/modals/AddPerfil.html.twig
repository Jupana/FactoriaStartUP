  <!--Ventana Pop-Up -->       
                <!-- Modal Header -->
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">
                        <span class="modal-close" aria-hidden="true">×</span>
                        <span class="sr-only">Close</span>
                    </button>
                </div>
                <div class="col-md-12 text-center m-t-10  modal-title">
                    <h4 class="par" id="myModalLabel">Nuevo Profile</h4>  
                </div>    
                <!-- Modal Body -->
                <div class="modal-body">               
                        {{ form_start (formAddProfile, { 'attr' : { 'id': 'profile_user'} } ) }}
                            {{ form_row (formAddProfile.sector) }}
                            {{ form_row (formAddProfile.profil) }}
                            {{ form_row (formAddProfile.description) }}                                             
                            {{ form_row (formAddProfile.submit) }}
                        {{ form_end (formAddProfile) }}
                </div>
                <!-- Modal Footer -->
                <div class="modal-footer">
                     <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                </div>                            
            
    
      