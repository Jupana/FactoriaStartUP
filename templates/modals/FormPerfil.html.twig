  <!--Ventana Pop-Up -->       
                <!-- Modal Header -->
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">
                        <span class="modal-close" aria-hidden="true">×</span>
                        <span class="sr-only">Close</span>
                    </button>
                </div>
                <div class="col-md-12 text-center m-t-10  modal-title">
                    <h4 class="par" id="myModalLabel">Nuevo Perfil</h4>  
                </div>    
                <!-- Modal Body -->
                <div class="modal-body">                  
                        {{ form_start (formProfile, { 'attr' : { 'id': 'profile_user' } }) }}
                            {{ form_row (formProfile.sector) }}
                            {{ form_row (formProfile.profil) }}
                            {{ form_row (formProfile.description) }}                                             
                            
                            <div class="row">
                                <div class="col-md-6 ">
                                     {{ form_row (formProfile.submit , { 'attr' : { 'class': 'btn-primary float-l' } }) }} 
                                </div>
                            {{ form_end (formProfile) }}
                                {# if sector is null it means that will create a new Profil , we will not show the Delete button#}
                                {% if profileUser.sector is not empty %}
                                <div class="col-md-6 ">
                                    <button type="button" class="btn-primary btn float-r profile_user_delete" data-id="{{profileUser.id}}">Borrar Perfil</button>
                                </div>
                                {% endif %}
                            </div>    
  
                </div>                          
            
    
      